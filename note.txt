Django Task for Software Engineer
Project: Payment Gateway Integration and Files Uploading System
Objective
Build a Django-based system where users can upload files only after completing a payment. The system
must support:
• File upload (after successful payment via aamarPay sandbox)
• Word count processing via Celery
• Payment and activity logging
• RESTful API interface
• Bootstrap-based frontend
• Django Admin for data inspection
• Dockerization



Functional Requirements
1.User Authentication
• Use Django's built-in user model.
• Implement login functionality using DRF Token Authentication or JWT.
• User registration/login is NOT the focus — use simple defaults if needed.
2. File Upload with Payment Requirement
• Users must pay ৳100 via aamarPay sandbox before uploading a file.
• After successful payment:
• A file upload form/API becomes available.
• Uploaded file is stored on disk.
• A Celery task is triggered to process the file.
• Files supported: .txt, .docx.



3.aamarPay Sandbox Integration
Use the aamarPay sandbox payment gateway:
 API Docs: https://aamarpay.readme.io/reference/initiate-payment-json
Use
• store_id: aamarpaytest
• signature_key: dbb74894e82415a2f7ff0ec3a97e4183
• endpoint: https://sandbox.aamarpay.com/jsonpost.php
• Required Endpoints:
• POST /api/initiate-payment/
• Initiates a payment request to aamarPay (৳100)
Returns the redirect URL to the aamarPay sandbox payment page
• GET /api/payment/success/
• Called by aamarPay after successful payment
• Creates a PaymentTransaction record
• Enables the file upload feature for the user


4. 🔁 Celery Background Task (Word Count)
After a successful upload:
A Celery task should:
• Read the file
• Count the number of words
• Save the word count to the database (FileUpload.word_count)
• Update the status to completed
• Log an ActivityLog entry
• For .docx, use python-docx; for .txt, use standard Python file read.


5. 🗃 Models
You must include at least the following models:
FileUpload
Field Type
user FK to User
file FileField
filename CharField
upload_time DateTimeField
status CharField (processing, completed, failed)
word_count PositiveIntegerField



PaymentTransaction
Field Type
user FK to User
transaction_id CharField
amount DecimalField
status CharField
gateway_response JSONField/Text
timestamp DateTimeField
ActivityLog
Field Type
user FK to User
action CharField
metadata JSONField
timestamp DateTimeField


6. 🧪 REST API Endpoints (DRF)
MethodEndpoint Description
POST /api/initiate-payment/ Create aamarPay payment request
GET /api/payment/success/ Callback from aamarPay after payment
POST /api/upload/ Upload a file after successful payment
GET /api/files/ List user’s uploaded files
GET /api/activity/ List user’s activity logs
GET /api/transactions/ List user’s payment history


7.Django Admin Panel
Enable admin access for:
• FileUpload
• ActivityLog
• PaymentTransaction
Staff should not be allowed to delete or update user-submitted uploads or payment data.


8.Frontend (Bootstrap UI)
Create a basic page (e.g., /dashboard/) that shows:
• A file upload form (only visible after successful payment)
• A table showing uploaded files and word counts
• A table showing user activity
• A table showing payment history
Use Django templates + Bootstrap. No JavaScript frameworks required.


9.Optional: Dockerization 
Add Docker support using:
• Dockerfile for the Django app
• docker-compose.yml including:
• Django app
• Redis (for Celery)
• MySQL (or use SQLite if preferred)
• .env file support for configuration










docker-compose run django